name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Guard against exposed secrets in VITE_ env vars
        run: |
          if grep -rE 'VITE_.*(SECRET|KEY|TOKEN|PASSWORD)' .env.example; then
            echo "::error::Sensitive variables must not use VITE_ prefix \u2014 they are embedded in the client bundle"
            exit 1
          fi

      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4
        with:
          version: 9
          run_install: false

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 20
          cache: 'pnpm'
          cache-dependency-path: 'package.json'

      - name: Install dependencies
        run: pnpm install

      - name: Lint
        run: pnpm lint

      - name: Type check
        run: pnpm type-check

      - name: Test
        run: pnpm test

      - name: Generate coverage
        id: coverage
        continue-on-error: true
        run: pnpm test:coverage

      - name: Check coverage threshold
        if: steps.coverage.outcome == 'success'
        run: |
          PCT=$(node --input-type=commonjs -e "
            const fs = require('fs');
            const summary = JSON.parse(fs.readFileSync('coverage/coverage-summary.json', 'utf8'));
            console.log(Math.round(summary.total.lines.pct));
          ")
          echo "Coverage: ${PCT}%"
          if [ "$PCT" -lt 90 ]; then
            echo "::error::Coverage ${PCT}% is below the 90% threshold"
            exit 1
          fi

      - name: Build
        run: pnpm build
